namespace Test
{
    public interface ITests
    {
        double Add(int count);
        double Update(int count);
        double Search();
        double Get(int count);
        double Delete(int count);
        void DeleteAll();
    }
}